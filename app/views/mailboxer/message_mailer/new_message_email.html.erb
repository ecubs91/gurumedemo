<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
  <% receiver = User.find(@message.receipts.pluck(:receiver_id)-[@message.sender.id]).first %>
    <h3>Hi <%= receiver.first_name %></h3>
    <p>You have a new message from <b><%= @message.sender.first_name.capitalize %></b> regarding possible tuition request. </p>
      
      
   
    <blockquote>
      <p style="border-left: 3px solid #f25842; padding-left: 12px;">
        <%= @subject %><br><br>
        <%= raw @message.body %><br>
        <span style="color:#757582">Sent on <%= @message.conversation.updated_at.strftime("%m/%e/%Y %I:%M %p") %></span>
      </p>
    </blockquote>
    <br>
    <br>
    <p>
      <%= link_to "Reply to #{@message.sender.first_name.capitalize}", conversation_url(:id => @message.conversation.id), :style => "background-color: #f25842;
    -moz-border-radius: 5px;
    -webkit-border-radius: 5px;
    border-radius: 4px;
    color: #fff;
    font-size: 20px;
    padding: 7px 10px 7px 10px;
    text-decoration: none;
    cursor: pointer;
    border:none;" %>
    </p>
  
  </body>
</html>
